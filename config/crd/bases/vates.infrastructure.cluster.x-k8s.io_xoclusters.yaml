---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: xoclusters.vates.infrastructure.cluster.x-k8s.io
spec:
  group: vates.infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: XOCluster
    listKind: XOClusterList
    plural: xoclusters
    singular: xocluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: string
    - jsonPath: .spec.controlPlaneEndpoint.host
      name: Endpoint
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: XOCluster is the Schema for the xoclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: XOClusterSpec defines the desired state of a Xen Orchestra
              cluster.
            properties:
              addons:
                description: |-
                  Addons selects which workload cluster addons the controller installs
                  via ClusterResourceSet. When nil, defaults to CCM and CSI enabled and
                  no CNI.
                properties:
                  ccm:
                    default: true
                    description: |-
                      CCM installs the Xen Orchestra cloud-controller-manager (node addresses,
                      service load balancers). Defaults to true.
                    type: boolean
                  cni:
                    description: |-
                      CNI selects the Container Network Interface installed via
                      ClusterResourceSet. Supported values: "none", "cilium". When unset
                      (or "none"), no CNI is installed by the controller.
                    enum:
                    - none
                    - cilium
                    type: string
                  csi:
                    default: true
                    description: CSI installs the Xen Orchestra CSI driver. Defaults
                      to true.
                    type: boolean
                type: object
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint is the endpoint for the control
                  plane.
                properties:
                  host:
                    description: Host is the DNS name or IP of the API server.
                    type: string
                  port:
                    description: Port is the port of the API server.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  subnet:
                    description: |-
                      Subnet is the CIDR subnet mask used by the control plane load balancer.
                      If not set, the load balancer implementation will auto-detect.
                    format: int32
                    maximum: 32
                    minimum: 1
                    type: integer
                required:
                - host
                - port
                type: object
              controlPlaneLB:
                description: |-
                  ControlPlaneLB selects the control plane load balancer implementation.
                  Supported values: "kube-vip". When set, the controller injects setup
                  scripts into the cloud-init data of control plane machines.
                enum:
                - kube-vip
                type: string
              identityRef:
                description: |-
                  IdentityRef is a reference to a Secret containing XO credentials.
                  The Secret must contain 'url' and 'token' data keys, and optionally
                  'insecure'. The Secret must be in the same namespace as the XOCluster.
                  If not set, falls back to the controller's global credentials.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              injectSSHKeys:
                default: false
                description: |-
                  InjectSSHKeys enables injection of the XO user's SSH public keys into
                  the cloud-init userdata of all VMs in this cluster. When enabled, the
                  controller fetches the SSH keys from the authenticated XO user profile
                  and merges them into the ssh_authorized_keys section of each VM's
                  cloud-config. Disabled by default to avoid modifying user-provided
                  bootstrap data without explicit opt-in.
                type: boolean
            type: object
          status:
            description: XOClusterStatus defines the observed state of XOCluster.
            properties:
              conditions:
                description: Conditions represent the current state of the XOCluster
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint represents the endpoint used to
                  communicate with the control plane.
                properties:
                  host:
                    description: Host is the DNS name or IP of the API server.
                    type: string
                  port:
                    description: Port is the port of the API server.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  subnet:
                    description: |-
                      Subnet is the CIDR subnet mask used by the control plane load balancer.
                      If not set, the load balancer implementation will auto-detect.
                    format: int32
                    maximum: 32
                    minimum: 1
                    type: integer
                required:
                - host
                - port
                type: object
              failureMessage:
                description: FailureMessage is the detailed error message (if any).
                type: string
              failureReason:
                description: FailureReason is the reason for the failure (if any).
                type: string
              initialization:
                description: |-
                  Initialization provides observations of the XOCluster initialization process.
                  NOTE: This field is part of the Cluster API contract and is used to orchestrate provisioning.
                properties:
                  provisioned:
                    description: |-
                      provisioned is true when the infrastructure provider reports that the resource is fully provisioned.
                      NOTE: This field is part of the Cluster API contract.
                    type: boolean
                type: object
              ready:
                description: Ready indicates the cluster infrastructure is ready.
                type: boolean
            type: object
        required:
        - metadata
        type: object
    served: true
    storage: true
    subresources:
      status: {}
