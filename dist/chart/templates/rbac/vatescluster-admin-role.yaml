{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "vates-capi.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "vates-capi.resourceName" (dict "suffix" "vatescluster-admin-role" "context" $) }}
rules:
- apiGroups:
  - vates.infrastructure.cluster.x-k8s.io
  resources:
  - clusters
  verbs:
  - '*'
- apiGroups:
  - vates.infrastructure.cluster.x-k8s.io
  resources:
  - clusters/status
  verbs:
  - get
{{- end }}
